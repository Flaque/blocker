# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'blocker' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for blocker
  pod 'Eureka', '~> 4.1'

  target 'blockerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'blockerUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end

target 'blocker-extension' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for blocker-extension

end
